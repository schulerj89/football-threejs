# Local-only ElevenLabs generation key.
# Do not rename this with a VITE_ prefix and do not expose it to browser code.
ELEVENLABS_API_KEY=

# Optional local announcer voice override for offline Node generation.
# This is not read by browser code.
FOOTBALL_ANNOUNCER_VOICE_ID=

# Local-only OpenAI image generation key for offline brand asset production.
# Do not rename this with a VITE_ prefix and do not expose it to browser code.
OPENAI_API_KEY=

# Local-only Meshy generation key for offline model production.
# Do not rename this with a VITE_ prefix and do not expose it to browser code.
MESHY_API_KEY=
